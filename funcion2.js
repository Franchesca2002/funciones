/* Crea una función que tome dos números como argumentos y devuelva el resultado de elevar el primero al segundo.
*/
function papel(b,g){
    return b**g
   
}

let numeral= parseInt(prompt("ingrese un numero"));
let numeral1=parseInt(prompt("ingrese un numero"));

let resultado=papel(numeral,numeral1);
  console.log(resultado);
 